<% provide(:title, "Statistics")%>

<% content_for :nav do %>
    <%= link_to 'Main Menu', static_pages_home_path %>
<% end %>

<h3>Battery statistics</h3>

<%= render partial: "batterystats", locals: { batteries: @batteries } %>

<h3>Pilot statistics</h3>
<%= render partial: "pilotstats", locals: { pilots: @pilots } %>

