%h3 Flight Log Users

-content_for :nav do
  =link_to 'Main Menu', static_pages_home_path
  =link_to 'New User', new_user_path

%table{:class => "table table-striped table-bordered"}
  %tr
    %th First name
    %th Last name
    %th Email
    %th{:width => "50"}
    %th{:width => "60"}

  - @users.each do |user|
    %tr
      %td= user.firstname
      %td= user.lastname
      %td= user.email
      %td= link_to 'Edit', edit_user_path(user), class: "btn btn-small btn-inverse"
      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: "btn btn-small btn-danger"

%br

